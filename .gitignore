# Local tooling (never commit dependencies)
tools/*/node_modules/
# Local-only staging tool (kept on disk, not tracked in git)
tools/staging/
.opencode/skills/furniture-staging/
# Downloaded ML models (auto-fetched on first run)
tools/staging/models/
# Local secrets (API keys) — never commit
.env
.env.*
